// Holds both the result of this battle and the current best run, so both can be returned together
public class BattleOutcome
{
    public GameResult Result { get; set; }
    public GameResult BestRun { get; set; }
}
